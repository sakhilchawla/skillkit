name: ship skill tests
skill: ./SKILL.md

scenarios:
  - name: checks branch before shipping
    invoke: "/ship"
    assertions:
      - contains: "NOT on main/master"

  - name: follows failure protocol
    invoke: "/ship"
    assertions:
      - contains: "Failure Protocol"
      - contains: "do NOT continue"
