name: ship skill tests
skill: ./SKILL.md

scenarios:
  - name: stops on main branch
    invoke: "/ship"
    assertions:
      - contains: "Cannot ship from main"

  - name: stops on test failure
    invoke: "/ship"
    assertions:
      - notContains: "git push"
