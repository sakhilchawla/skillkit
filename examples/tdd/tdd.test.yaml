name: tdd skill tests
skill: ./SKILL.md

scenarios:
  - name: writes test before implementation
    invoke: "/tdd add a sum function"
    assertions:
      - contains: "test"
      - contains: "fail"

  - name: follows RED GREEN REFACTOR cycle
    invoke: "/tdd add a sum function"
    assertions:
      - contains: "RED"
      - contains: "GREEN"
      - contains: "REFACTOR"
      - completes: true
