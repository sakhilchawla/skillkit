{
  "name": "@skillkit/linter",
  "version": "0.1.0",
  "description": "Lint and validate Agent Skills against the spec and best practices",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@skillkit/core": "*"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT"
}
